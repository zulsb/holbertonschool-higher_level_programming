#!/usr/bin/node
// This script that prints 3 lines using an array of string and a loop.
const arr = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let a = 0; a < arr.length; a++) {
  console.log(arr[a]);
}
